@model WebBookStore.Models.Book
@{
    ViewData["Title"] = "Xóa sách";
}

<h2>Xác nhận xóa sách</h2>

<div>
    <p><strong>Tiêu đề:</strong> @Model.Title</p>
    <p><strong>Giá:</strong> @Model.Price.ToString("N0") ₫</p>
    <p><strong>Tác giả:</strong> @Model.Author?.Name</p>
    <p><strong>Thể loại:</strong> @Model.Category?.Name</p>
    <p><strong>NXB:</strong> @Model.Publisher?.Name</p>
    <p><img src="@Model.CoverUrl" alt="Ảnh bìa" width="120" /></p>
</div>

<form asp-action="DeleteConfirmed">
    <input type="hidden" asp-for="Id" />
    <p>Bạn có chắc chắn muốn <strong>xóa mềm</strong> sách này không? Dữ liệu sẽ không bị mất, chỉ bị ẩn khỏi danh sách.</p>

    <button type="submit" class="btn btn-danger">Xác nhận xóa mềm</button>
    <a asp-action="Index" class="btn btn-secondary">Hủy</a>
</form>
